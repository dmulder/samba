<samba:parameter name="winbind password kinit"
                 context="G"
                 type="list"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>

	<para>This parameter controls which users for which Winbind will use
	cached passwords to refresh Kerberos Tickets retrieved using the
	<parameter moreinfo="none">pam_winbind</parameter> module. This option
	requires that <smbconfoption name="winbind refresh tickets"/> also be
	enabled. Accepts either a list of UPNs of users, or * meaning all.

</para>
</description>

<value type="default">*</value>
<value type="example">tux@example.com, affe@example.com</value>
</samba:parameter>
